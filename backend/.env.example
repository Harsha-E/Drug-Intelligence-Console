# API Configuration
PORT=8000
HOST=0.0.0.0

# Registry Configuration
# The path where compiled registry JSONs are stored.
# If omitted, defaults to '../registry/current' relative to the backend directory.
REGISTRY_DIR=../registry/current

# Deployment Info
BUILD_VERSION=1.0.0
GIT_COMMIT=unknown
