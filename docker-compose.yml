version: '3.8'
services:
  drug-intelligence-cloud:
    build: .
    ports:
      - "7860:7860"
    environment:
      - HOST=0.0.0.0
      - PORT=7860
    volumes:
      - ./backend/registry:/app/backend/registry
